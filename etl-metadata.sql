CREATE TABLE IF NOT EXISTS `production-dashboard-482014.dashboard_data.etl_metadata` (
    process_name STRING,
    last_sync_attempt DATETIME,
    rows_affected INT64,
    status STRING,
    details STRING
);
